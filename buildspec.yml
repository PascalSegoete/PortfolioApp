version: 0.2
phases:
 install:
 runtime-versions:
 nodejs: 12
 commands:
 - echo "Installing Dependencies..."
 - npm install 
 - npm install -g @angular/cli@10.0.8
 build:
 commands:
 - echo "Building Angular Project..."
 - ng build 
 post_build:
 commands:
 - echo "Updating files in the S3 bucket..."
 - aws s3 sync dist/career-platform/ s3://career-profile --delete